---
export interface Props {
  name: string;
}

const { name } = Astro.props as Props;

// FIXME: SVG としてアイコンを読み込む
// const { default: innerHTML } = await import(`/assets/${name}.svg?raw`);
---

<!--
<Fragment set:html={innerHTML} />
-->

<img alt="" src={`/assets/${name}.svg?raw`} width={24} height={24}>
